baseURL = ""
relativeURLs = true
uglyURLs = true
title = "AI Best Practices Knowledge Base"
languageCode = "en-us"
publishDir = "docs"

[taxonomies]
  tag = "tags"

[params]
  description = "What actually works in AI-assisted development — synthesized from 6,000+ practitioner comments across 20 major HN discussions."
  author = "AI Best Practices Knowledge Base"

[markup]
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 3
    ordered = false
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[outputs]
  home = ["HTML", "RSS", "JSON"]

[outputFormats.JSON]
  mediaType = "application/json"
  baseName = "search-index"
  isPlainText = true
